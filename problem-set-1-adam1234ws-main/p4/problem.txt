Write a program that reads integers from the standard input until it reads in a negative integer which signals it to stop. 
The program should then print the smallest and second smallest integers seen in the input.

Input:
Collect one integer per line from stdin until the user enters a negative integer.
Output:
Print the smallest and second smallest integers.